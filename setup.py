# coding: utf-8

import setuptools


NAME = 'lazy_python_repo'
VERSION = '0.0.0'
REQUIRES = [

]


setuptools.setup(
    name=NAME,
    version=VERSION,
    install_requires=REQUIRES,
    packages=setuptools.find_packages()
)
